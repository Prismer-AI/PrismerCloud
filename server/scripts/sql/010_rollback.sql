-- =============================================================================
-- Prismer Cloud - 前端先行表回滚脚本
-- 版本: 1.0.0
-- 日期: 2026-01-25
-- 
-- ⚠️ 警告：此脚本会删除 pc_ 前缀的表和数据
-- ✅ 安全：不会影响任何现有表 (users, api_keys, usage_records 等)
-- =============================================================================

USE prismer_info;

-- =============================================================================
-- 删除前端先行表 (仅 pc_ 前缀)
-- =============================================================================

DROP TABLE IF EXISTS pc_credit_transactions;
SELECT 'pc_credit_transactions 已删除' AS result;

DROP TABLE IF EXISTS pc_usage_records;
SELECT 'pc_usage_records 已删除' AS result;

DROP TABLE IF EXISTS pc_user_credits;
SELECT 'pc_user_credits 已删除' AS result;

-- =============================================================================
-- 完成
-- =============================================================================
SELECT '✅ 回滚完成 (仅删除 pc_ 前缀表)' AS result;
